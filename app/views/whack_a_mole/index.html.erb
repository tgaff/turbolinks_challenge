<h1>WhackAMole#index</h1>
<p>You should be able to delete each mole without the page reloading.</p>

<div class="alert alert-warning" role="alert">
  This page was last loaded at <strong><%= @last_load_time %></strong>.
      If this changes, you reloaded the page...
</div>
<div class="alert alert-info" role="alert">
  There are currently <strong><%= @moles.count %></strong> moles.
</div>


<div class="row">
  <% @moles.each do |mole| %>
    <div class="col-sm-6 col-md-4">
      <div class="thumbnail">
        <%= image_tag 'mole.png' %>
        <div class="caption">
          <h3><%= mole[:name] %></h3>
          <p><%= link_to 'Delete', mole_path(mole), method: 'delete', class: "btn btn-primary"%></p>
        </div>
      </div>
    </div>
  <% end %>

</div>
